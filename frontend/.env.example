# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:8000

# Better Auth Configuration (if used)
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:8000
# NOTE: In a real implementation, the JWT secret would be set on the server side
# and would not be exposed in the frontend. This is just for reference.